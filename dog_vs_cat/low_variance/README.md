在high_variance中的模型表现不是很理想，因为模型出现了过拟合现象，在low_variance_model中，我们加入了正则化来防止过拟合。
解决过拟合的方法如下：
（1）数据增强
（2）增加Dropout层
（3）增加正则化项
（4）降低网络复杂度
（5）早停（early stopping）

在此次模型中我们选用了数据增强和增加Dropout层来防止过拟合现象，网络结构如下：

预测结果为：

	
曲线图在文件夹中。
由此可见，使用了数据增强和Dropout层后，模型不再过拟合，训练曲线紧紧跟随着验证曲线，现在的精度